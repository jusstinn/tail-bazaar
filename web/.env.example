
# Hosted mode: comma-separated order ids whose evidence is publicly viewable as DEMONSTRATION FIXTURES (badge shown); all other orders stay behind the buyer session / operator token.
DEMO_PUBLIC_ORDERS=
# Or auto-publish the tampered demo fixture and its paired valid order (1 to enable).
DEMO_PUBLIC_TAMPER_FIXTURES=
