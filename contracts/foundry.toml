[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
via_ir = false
fs_permissions = []
# No remappings: this project deliberately has no external Solidity dependencies
# (the test base contract declares the small Vm interface it needs).
